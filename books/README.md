# Books

Large PDF files are stored externally:

- [In the Palm of Your Hand (Poet's Portable Workshop)](https://your-storage-link-here) 
- use `git lfs install` to initiliaze git lfs
- use `git lfs track "*.pdf"` to track all git lfs